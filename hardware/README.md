# hardware

PCB and mechanical design files.

Configure Git LFS before the first binary lands here, not after. Altium and
Fusion files are large and version poorly as plain blobs.

Fusion 360 has no git-native format. Version exported STEP or F3D files
rather than fighting the cloud workspace.

Ignore output, cache and backup artefacts in `.gitignore`.
